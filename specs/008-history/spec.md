在添加了jsonl持久化数据功能后，在历史页面，显示所有已保存的转录数据


- 左侧是转录列表，右侧是转录内容预览
- 转录列表中，显示文件名和创建时间（从metadata中获取）、条目数
- 提供修改文件名、删除文件的功能
- 转录预览中，参考主页面的历史转录区域UI
- 预览右上角添加一个导出按钮（这个按钮位置要固定，你可以设计一个合适的固定区域，或者直接使用macos26 liquid glass提供一个固定的按钮），支持导出多种格式（srt和markdown）
