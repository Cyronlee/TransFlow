## 视频播放控件

- [x] 调整视频播放器的控件尺寸，高度减少，宽度增加，并移除"其他设备播放按钮"

## 优化字幕浮动窗口的UI

- [x] 调整按钮位置，顺序为'调整字号''置顶''关闭'

- [x] 现在几个按钮会占据右边的区域，导致左侧的文本无法使用右边一部分的空间，改为浮在文本上方

- [x] "调整字号"从两个按钮改为一个，使用`textformat.size`图标（不用国际化），点击后在下拉选提供比较多的字号，最小12pt，最大72pt

- [x] hover在边框上时显示拖拽鼠标图标

## 修复创建新会话bug

- [x] 修复点击主页面，点击+号后，预填充了文件名，但是创建按钮无法点击的问题

## 跟随滚动

- [x] 在已转录区右下角添加一个'跟随滚动'的圆形图标（chevron.down.2）toggle按钮，默认激活

## 修复崩溃问题

- [x] 修复视频转写历史预览在DMG安装后崩溃的问题（`_AVKit_SwiftUI` metadata resolution failure）
- [x] 修复实时转写+翻译进行中切换到历史页面导致崩溃的问题（`TranslationSession` 生命周期）
